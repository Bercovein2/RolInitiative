<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toolbar"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="?attr/colorPrimary"
    android:elevation="4dp"
    android:theme="@style/ThemeOverlay.AppCompat.ActionBar"
    android:popupTheme="@style/ThemeOverlay.AppCompat.Light">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_alignParentStart="true"
            android:text="@string/app_name"
            android:textColor="@android:color/white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginStart="0dp"/>

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_toEndOf="@id/toolbar_title"
            android:layout_toStartOf="@id/buttonRollDice"/>

        <ImageButton
            android:id="@+id/buttonToggleTimer"
            android:layout_toStartOf="@id/buttonFolders"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_button_cronometer"
            android:contentDescription="@string/show_timer"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp" />

        <ImageButton
            android:id="@+id/buttonFolders"
            android:layout_toStartOf="@id/buttonNotes"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_folder"
            android:contentDescription="@string/show_popup_notes"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp" />

        <ImageButton
            android:id="@+id/buttonNotes"
            android:layout_toStartOf="@id/buttonRollDice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_button_note"
            android:contentDescription="@string/show_popup_notes"
            android:layout_centerVertical="true"
            android:layout_marginEnd="16dp" />

        <ImageButton
            android:id="@+id/buttonRollDice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_alignParentEnd="true"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/show_popup_dice"
            android:src="@drawable/ic_button_dice" />



    </RelativeLayout>
</androidx.appcompat.widget.Toolbar>



